import React from 'react';
import heroImage from '../../resources/Hero.png'

const Hero = () => (
      <img src={heroImage} 
      alt="Man standing infront city scape"
      id='hero-img' 
      />
);

export default Hero;